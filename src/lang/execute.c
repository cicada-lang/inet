#include "index.h"

void
execute(const op_t *op, worker_t *self, frame_t *frame) {
    // switch (op->tag) {
    // case OP_CALL_PROGRAM:
    //     return;
    // case OP_CALL_NODE
    // case OP_CONNECT
    // case OP_GET_FREE_PORT
    // }
    (void)op;
    (void)self;
    (void)frame;
}
